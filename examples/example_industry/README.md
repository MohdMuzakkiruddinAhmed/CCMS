# Industry Example

This example demonstrates CCMS usage on an industry-scale entity resolution scenario.

## Usage

```bash
python run_industry.py er1.csv er2.csv
```

Replace `er1.csv` and `er2.csv` with your actual ER output files.
Each file must have two columns: `RecID` and `ClusterID`.
